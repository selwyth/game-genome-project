DATABASE_URL=postgresql://davidfeng@localhost/ggp
ANTHROPIC_API_KEY=sk-ant-...
ADMIN_TOKEN=change-me-to-a-secret-token

# Password required to submit a new game for AI classification. Leave empty to disable.
UPLOAD_PASSWORD=

# Taxonomy file location. Defaults to ../taxonomy.yaml (repo root, relative to backend/).
# TAXONOMY_PATH=../taxonomy.yaml

# Path to built React frontend. Set this in Railway to ../frontend/dist
# STATIC_DIR=../frontend/dist
